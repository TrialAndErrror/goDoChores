package views

import (
	"fmt"
	"goDoChores/routes"
)

templ remindersList(reminders []ChoreReminderListEntry) {
	@base("chores") {
		<div class="card" data-bs-theme="dark">
			<h2 class="card-header" style="padding: 1.5rem">
				All Chore Reminders
			</h2>
			<div class="card-body row">
				for _, el := range reminders {
					<div class="col-4 p-2">
						<a href={ templ.SafeURL(routes.URLFor("remindersDetail", el.ID)) } class="link-dark">
							<div class="chore-card">
								{ fmt.Sprintf("%d", el.Name) }
							</div>
						</a>
					</div>
				}
			</div>
		</div>
	}
}
